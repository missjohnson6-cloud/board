# Standing Manifest — phone board

A single static page. Signs in with the phone login; ticks things off, adds items, filters by stream. Data lives in Supabase, never here. The key in the page is the publishable key, which is public by design and reads nothing without a sign-in.
Source of truth: Atrium-Core/automation/mac/tools/manifest/web/
